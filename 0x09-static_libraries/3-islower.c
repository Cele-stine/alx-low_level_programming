#include "main.h"

/**
 * _islower - checks if a char i lower case or not
 * @c: an int representing the char to be checked
 * Return: 1 if c is lowercase, 0 otherwise
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
